{
  "spec_id": "generic_csv_default",
  "adapter_id": "generic_csv",
  "version": "1.0.0",
  "label": "Generic CSV Default",
  "description": "Default chain for importing a user-supplied CSV through configurable workbench transforms.",
  "steps": [
    {
      "id": "finalize_output",
      "operation": "finalize_output",
      "label": "Finalize output",
      "description": "Preserve imported columns as output fields without adding canonical placeholders.",
      "outputs": ["clean records"],
      "params": {
        "drop_fields": [],
        "canonical_fields_first": false,
        "add_missing_canonical_fields": false
      }
    }
  ]
}
