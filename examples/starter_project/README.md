# GLADR Starter Project

This is a sanitized project template. Copy this directory outside version
control or into an ignored `projects/` folder, then add local data and run
artifacts there.

Expected local folders:

```text
data/
outputs/
specs/
notebooks/
deliverables/
```
